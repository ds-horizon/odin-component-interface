apiVersion: v1
kind: Service
metadata:
  name: {{ serviceName }}
  labels:
    run: {{ baseConfig.name }}
spec:
  ports:
    - port: {{ flavourConfig.external_port }}
      protocol: TCP
  selector:
    run: {{ baseConfig.name }}
